void main() {
  print("Hello, World!");
}
/* Dart follows the same syntax as C, C++, and Java.
   The main() function is the entry point of a Dart program.
   The print() function is used to print the output to the console.
   The semicolon (;) is used to terminate the statement.
   The // is used to write a single-line comment.
   The /*...*/ is used to write a multi-line comment.
   The Dart code is case-sensitive.
   The Dart code is written in a file with the .dart extension.
   The Dart code is executed using the Dart VM or DartPad.
   The Dart code is compiled into JavaScript code using the dart2js compiler.
   DartPad is an online editor that allows you to write, compile, and run Dart code in the browser.
   string concatenation in Dart is done using the + operator or the $ symbol.
   The + operator is used to concatenate two strings.
   The $ symbol is used to interpolate a variable or an expression in a string.
   The Dart code is organized into classes, functions, and variables.
   The Dart code is organized into packages, libraries, and files.
   The Dart code is organized into modules, directories, and projects.
   String concatenation is the process of combining two or more strings into a single string. */